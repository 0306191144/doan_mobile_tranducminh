import 'dart:html';

import 'package:flutter/material.dart';

class Invoice_status extends StatefulWidget {
  const Invoice_status({Key? key}) : super(key: key);

  @override
  State<Invoice_status> createState() => _Invoice_statusState();
}

class _Invoice_statusState extends State<Invoice_status> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SingleChildScrollView(),
    );
  }
}
